interface:
  display_name: "/xxd-panel-022"
  short_description: "Create clean-line breakout editorials with one accent."
  default_prompt: "Use $xxd-panel-022 to place my source-bound subject mostly inside one pure-black rectangle with one defining feature breaking the boundary, clean smooth stable hand line, white negative form, tiny grey planes, exactly one small source-derived accent colour, asymmetric white space, and native-script editorial annotations; ask first for modes and copy or locale preferences."
